// let width = prompt("enter image width with px")
// let hight = prompt("enter image hight with px")
// alert("Are you sure to change width and Hight")

// let img = document.getElementById("myImage")
// img.setAttribute("width",width)
// img.setAttribute("hight",hight)

        
// let userWidth = prompt("Enter the width (in pixels):");
// let userHeight = prompt("Enter the height (in pixels):");

// let img = document.getElementById('myImage');

// if (userWidth && userHeight) {
//     img.style.width = userWidth + "px";
//     img.style.height = userHeight + "px";
//  }
